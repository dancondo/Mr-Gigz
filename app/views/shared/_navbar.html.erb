<nav class="navbar navbar-expand-lg navbar-dark">
  <p class="navbar-brand" id="navbar-brand"><%= link_to 'LOGO Mr.Gigz', root_path %></p>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <% if user_signed_in? %>
          <% if current_user.band %>
            <%= link_to current_user.band, class: "nav-link" do %>
              <%= current_user.band.name %>
            <% end %>
          <% elsif current_user.bar %>
            <%= link_to current_user.bar, class: "nav-link" do %>
              <%= current_user.bar.name %>
            <% end %>
          <% else %>
            <%= link_to 'Bem vindo', '#', class: 'nav-link' %>
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: "nav-link" do %>
            Login
          <% end %>
        <% end %>
      </li>

      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to destroy_user_session_path, method: :delete, class: "nav-link" do %>
            Logout
          <% end %>
        <% else %>
          <%= link_to new_user_registration_path, class: "nav-link" do %>
            Sign up
          <% end %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
