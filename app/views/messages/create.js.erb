<% if @message.errors.any? %>

<% else %>
  var messages = document.querySelector('.messages-all');
  messages.insertAdjacentHTML('beforeend', "<%= j render 'show', m: @message %>");
  var inputBox = document.querySelector('.form-control');
  inputBox.value = "";
  inputBox.blur();
<% end %>
