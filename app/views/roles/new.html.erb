<div class="container">
	<div class="row">
		<div class="col-xs-12 col-sm-12 form-margin-role">
			<div class="form-post">
				<h1>Você representa uma banda ou bar?</h1>
				<%= simple_form_for current_user, url: roles_path, method: :post do |f| %>
				<div class="row">
					<div class="col-xs-12 col-sm-12">
						<div class="form-group">
							 <%= f.input :role, collection: {'Banda' => :band, 'Bar' => :bar} %>
						</div>
					</div>
				</div>
				<div class="form-group caixa-bttn">
					<%= f.submit 'Próximo', class: "btn btn-primary" %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</div>
