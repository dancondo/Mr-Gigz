<%= simple_form_for @bar do |f| %>
  <%= f.input :name, label: 'Atualize o nome do bar' %>
  <%= f.input :address, label: 'Atualize o endereço' %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'Atualize', class: "btn btn-primary" %>
<% end %>
